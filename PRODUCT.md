# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**1순위 — 진학 희망 학생.** 석사·박사 지원을 고민 중이거나 학부연구생 자리를 찾는 학생이다.
대개 학과 홈페이지나 검색으로 흘러들어와, 짧은 시간 안에 세 가지를 판단하려 한다:
이 랩이 실제로 뭘 하는지, 지금도 살아 있는지, 그리고 자기가 들어갈 만한지.
전산·통계 배경이 많고 생물학은 모르는 경우가 흔하다 — 모집 문구가 이미 그렇게 쓰여 있다.

2순위로 기록해 두는 방문자(설계 시 1순위와 충돌하면 1순위가 이긴다):

- **동료 연구자·과제 심사위원** — 최근 실적과 저널 수준을 빠르게 확인한다. 저자 역할(1저자·교신)과 IF 가 이들을 위해 존재한다.
- **재학생·랩 내부** — 갤러리, 수상 이력, 강의 목록을 본다. 사이트가 랩의 기록 보관소 역할을 겸한다.
- **학과·대학 행정** — 공식 표기와 연락처의 정확성을 본다.

## Product Purpose

명지대학교 인공지능 데이터분석 연구실(Artificial Intelligence and Data Analytics Lab, 지도교수 오민식)의
공개 기록이다. 연구 주제, 논문, 구성원, 수상, 소식, 행사 사진, 강의, 지원 안내를 한 곳에 둔다.

**성공은 두 가지로 판단한다.**

1. 진학을 고민하던 학생이 이 사이트만 보고 연락할 근거를 얻는다.
2. **사이트가 낡지 않는다.** 이전 사이트는 2024년에서 멈췄고 멈춘 줄 아무도 몰랐다.
   이 사이트의 존재 이유의 절반은 그 실패를 반복하지 않는 것이다.

## Positioning

**방법론을 만드는 랩이다.** 단일세포 전사체와 공간 전사체 데이터를 위한 기계학습 모델·알고리즘 자체를
설계한다 — 세포 표현학습, 세포 유형 주석, 공간 발현 예측, 약물 반응 모델링.
남이 만든 파이프라인을 적용하는 랩이 아니라, 그 파이프라인이 되는 방법을 내놓는 쪽이다.

이것이 확인된 차별점이다. 도구 배포와 생물·임상 응용은 실제로 하는 일이지만(아래 Evidence 참조)
랩을 규정하는 주장으로 쓰지 않는다.

## Operating Context

- **콘텐츠는 전부 `content/*.yaml` 이고 지도교수가 직접 고친다.** 학생에게 편집 권한을 주는 방식은
  검토했고 채택하지 않았다 — 학생이 알려주면 지도교수가 반영한다.
- **예외는 행사 사진 하나다.** 학생이 GitHub 웹에서 `raw-photos/<앨범ID>/` 에 드래그하면
  워크플로가 리사이즈·EXIF 제거·YAML 갱신·원본 삭제까지 하고 배포까지 이어진다.
- **갱신은 사건 단위로 일어난다.** 논문 accept 즉시, 학기 시작, 학위수여식, 수상 발표, 행사 직후.
  정기적으로 들여다보는 사람은 없다 — 그래서 낡음이 사이트 스스로 드러나야 한다.
- 원본 사진과 학교 내부 시스템 export(`pro.html`)는 저장소에 두지 않는다.
- 저장소는 공개다. 구성원 이메일이 YAML 에 평문으로 들어 있고, 이는 확인된 결정이다.

## Capabilities and Constraints

**기술 제약(바꿀 수 없는 것)**

- Next.js App Router + `output: 'export'` 정적 사이트, GitHub Pages 배포.
  서버 런타임이 없다 — 런타임 fetch, 라우트 핸들러, `next/image` 최적화를 쓸 수 없다.
- **크론 워크플로를 두지 않는다.** GitHub 는 공개 저장소에서 60일간 커밋이 없으면 스케줄
  워크플로를 자동 비활성화한다. 이전 사이트가 죽은 원인이다. 빌드는 push 에만 반응한다.
- keepalive · 실패 알림 · 하트비트를 붙이지 않는다. 하나를 살리려고 셋을 더 붙이는 구조가 된다.
- `basePath` 를 손으로 적지 않는다. 배포 워크플로가 저장소 이름에서 파생한다.
- 이미지는 사전 리사이즈된 정적 파일만 쓴다.

**낡음을 드러내는 장치(제거하면 안 되는 것)**

- 푸터의 `Last updated` 는 최종 커밋 날짜다. 재배포로는 갱신되지 않는다 — 그게 목적이다.
- 확정 논문 중 최신 것이 12개월 이상 지나면 푸터에 경고가 뜬다.
- venue 미매칭 · 주제 미분류 · 수상자 미매칭이 푸터와 해당 페이지에 경고로 나온다.

**파생 규칙(숫자를 하드코딩하지 않는다)**

- 카운트, 연도 목록, 개인 논문 목록, 저자 굵게 처리, 개인 페이지 생성 여부, 앨범·학기 정렬이
  전부 `lib/content.ts` 에서 파생된다.
- 멤버 개인 페이지는 **논문이나 수상이 하나라도 있는 사람에게만** 생긴다.
  이름만 있는 빈 페이지는 없는 것보다 나쁘다. 사람이 켜고 끄는 플래그를 만들지 않는다.
- IF·SCIE 는 논문이 아니라 저널의 속성이라 `venues.yaml` 에서만 관리한다.
- 수상 소식은 `awards.yaml` 에서 뉴스 항목으로 파생된다. 두 벌로 적지 않는다.

**시각 체계**

- **밝은 톤 한 벌만 쓴다. 다크모드를 만들지 않는다.** 목업(`mju-ailab-site.html`)이
  기준이고 거기서 정해진 결정이다. 감사 도구가 "다크모드 없음"을 매번 지적하겠지만
  결함이 아니라 선택이다 — 뒤집으려면 목업부터 다시 정해야 한다.
- 제목·라벨 글꼴은 빌드 타임에 받아 자체 호스팅한다(`next/font`). 본문 한글
  (Pretendard)만 CDN 에 남아 있고, 그쪽이 막혀도 시스템 한글 글꼴로 떨어진다.

**언어**

- 기본은 영문. 한국어는 네 군데뿐이다: 홈 소개 문단, 모집 안내·체크리스트,
  국내 학회 논문 제목(원문 그대로 — 번역하면 인용이 어긋난다), 상·지원사업 이름.
- **언어 전환 토글을 만들지 않는다.** 콘텐츠 두 벌 관리는 유지보수 실패의 원인이 된다.

**미정으로 남은 것**

- 도메인: 현재 `ailab-mju.github.io`. `ailab.mju.ac.kr` 로 옮길지 미정
  (옮기면 DNS + `CNAME` + `lab.yaml` 의 `site_url` 을 함께 바꿔야 한다).
- 현재 정식 저장소는 `ailab-mju/site-next` 이고, 라이브인 `ailab-mju.github.io` 를
  언제 어떻게 덮어쓸지 미정이다.

## Brand Commitments

- **이름:** `Artificial Intelligence and Data Analytics Lab` (전체), `MJU AI Lab` (약칭).
- **소속 표기:** `Department of Artificial Intelligence, Myongji University` — **확정.**
  명지대 공식 조직도(인공지능·소프트웨어융합대학 > 융합소프트웨어학부 > 인공지능전공)와
  일치하지 않지만, 짧고 해외 독자가 바로 이해한다는 이유로 이쪽을 택했다. 이 문자열은
  헤더·주소·메타데이터에 그대로 나간다. 되돌릴 때는 세 곳을 함께 바꾼다.
- **태그라인:** `Machine learning methods for single-cell and spatial transcriptomics.`
- **모집 약속:** 생물학 배경은 필수가 아니라고 명시한다. 보통 3일 안에 답장한다고 적혀 있다.
  이 두 문장은 지원자와의 약속이므로 임의로 지우거나 약화하지 않는다.
- **연락처:** `msoh@mju.ac.kr` · 종합관 1820호 · 02-300-0679 (명지대 교수 프로필에서 확인).
- 구성원 이메일은 화면에 평문으로 찍지 않고 mailto 링크로만 렌더링한다.

## Evidence on Hand

**실재하는 콘텐츠(전부 확인된 값. `content/*.yaml`)**

| 항목 | 수량 |
|---|---|
| 논문 | 33건 (2017–2026) — 해외 저널 16 · 해외 학회 1 · 국내 저널 1 · 국내 학회 15 |
| 저널·학회 | 15곳, 그중 13곳에 JCR 2025 IF |
| 구성원 | 29명 — PI 1 · 석사 6 · 인턴 3 · 졸업생 19 |
| 수상·선정 | 8건 — 수상 6 · 지원사업 선정 2 |
| 연구과제 | 7건 (NRF · KOCCA · KHIDI · KEIT) |
| 행사 사진 | 6개 앨범 45장 (2024–2026) |
| 강의 | 10개 학기 |
| 소식 | 8건 (+ 수상 8건이 파생되어 합쳐진다) |

**없는 것 — 지어내지 말 것**

- **홈에 대표 이미지가 없다.** 히어로 슬롯은 비어 있어서 지웠다 — 랩을 대표할 사진을
  고르는 것은 사람이 할 일이라 지어내지 않았다. 넣기로 하면 홈 상단에 자리를 다시 만든다.
  (OG 카드는 `npm run make-og` 로 글자만으로 만든다 — 사진이 아니다.)
- 추천사·후기·수강생 평가·순위·지표가 하나도 없다. 만들어 넣을 근거가 없다.
- 2024년 이전 강의 목록은 이전 사이트에서 옮겨온 것으로 출처 확인이 안 됐다.
- 연구과제 1건(DRL 다중오믹스)의 기간·지원기관 미상.
- 논문 세부 미확인: JKMS 게재월, 한국정보과학회 15건의 `pages`,
  scFANCL(BMC Genomics) 교신저자.
- 인턴 2명(임준형·서윤아)의 한글 이름·이메일·사진, 졸업생 다수의 현재 소속(`next`).

## Product Principles

1. **낡았다는 사실을 숨기지 않는다.** 경고와 커밋 날짜는 결함이 아니라 기능이다.
   보기 싫다고 지우면 이 사이트는 이전 사이트가 죽은 방식 그대로 죽는다.
2. **파생하고 중복하지 않는다.** 같은 사실을 두 곳에 적으면 한쪽만 고쳐져 어긋난다.
   숫자·목록·분류는 언제나 하나의 YAML 에서 계산된다.
3. **손이 가는 곳을 줄인다.** 갱신 한 번에 고쳐야 할 파일이 늘어나는 설계는 채택하지 않는다.
   졸업 처리는 세 필드, 새 논문은 한 항목으로 끝나야 한다.
4. **진학 희망 학생이 판단할 수 있게 한다.** 무엇을 연구하는지, 지금 누가 있는지,
   나가서 어디로 갔는지, 어떻게 연락하는지 — 이 네 가지가 막히면 안 된다.
5. **원문을 고치지 않는다.** 국내 학회 논문 제목, 상 이름, 저자 표기는 원문 그대로 둔다.
   보기 좋게 다듬는 순간 인용과 검색이 어긋난다.

## Accessibility & Inclusion

- 접힘 목록은 JS 없이 동작하는 네이티브 `<details>/<summary>` 로 되어 있다.
  정적 사이트라 JS 가 죽어도 본문은 읽혀야 한다.
- **미정:** 국내 대학 사이트에 적용되는 웹 접근성(KWCAG) 준수 의무가 이 사이트에 걸리는지
  확인되지 않았다. 요구 수준이 정해지면 여기에 기록한다.
